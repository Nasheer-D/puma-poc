export interface CreditPackage {
    packageID: string;
    amount: number;
    bonusCredits: number;
    bonusTickets: number;
    featured: boolean;
    priceInUSD: number;
    priceInPMA: number;
}
